[package]
name = "corral-tui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
corral-client = { workspace = true }
corral-protocol = { workspace = true }
# Raw mode and the terminal's own size: no standard-library API exposes either.
# Already this surface's dependency before the crate existed — the attach loop
# moved here with it.
rustix = { workspace = true }
tokio = { workspace = true }


[dev-dependencies]
# Snapshot coverage for what a person actually sees (workflow §6).
insta = { workspace = true }
# Rows in the shapes a daemon sends, for the list's decoding tests.
serde_json = { workspace = true }

[lints]
workspace = true
