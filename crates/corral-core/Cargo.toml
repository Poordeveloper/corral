[package]
name = "corral-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
# Corral-minted identity. Nothing else here needs a dependency: the domain is
# types and invariants, and it deliberately owns no encoding.
uuid = { workspace = true }

[lints]
workspace = true
